//? DOM - Document Object Model
//? The DOM is object oriented meaning that node has its own set of properties and methods that we can change we can add we can remove. The browser give a Window Object and inside that we have a Document Object. And this represents just that the loaded document or web page. It is structured representation of an HTML document. It can be thought of kind of as a tree of nodes or Elements created by the browser.

let n = 125;
let mask = 1;
let m = n & mask;
console.log(m);
//? Bitwise operation
