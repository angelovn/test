function substring(word, start, finish) {
  let endIndex = finish + start;
  console.log(`${word.substring(start, endIndex)}`);
}
substring("ASentence", 1, 8);

//? from the the 1st index we need to get eight symbols
