function sort(n1, n2, n3) {
  let arr = [n1, n2, n3];
  for (let i = 0; i < arr.lenth - 1; i++) {
    let current = arr[i];
    if (current < previous) {
      let previous = current;
    }
  }
}
sort(2, 1, 3);
